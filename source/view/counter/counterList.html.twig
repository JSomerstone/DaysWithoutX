<h2>{{ list_headline }}</h2>
<table class="counter-list">
    <tbody>
        {% for counter in counters %}
            <tr>

                <th class="counter-list-daycount">
                    {{ counter.days }}
                </th>
                <td class="counter-list-head">
                    <a href="/{{ counter.name }}/{{ counter.owner }}">
                        days without <b>{{ counter.headline }}</b>
                    </a>
                </td>
                <td>
                    {% include 'counter/visibilityIcon.html.twig' with { counter: counter } %}
                </td>
                {% if counter.owner and showUser %}
                <td>
                    <a href="/user/{{ counter.owner }}">
                        {{ counter.owner }}
                    </a>
                </td>
                {% endif %}
            </tr>
        {% endfor %}
    </tbody>
</table>
