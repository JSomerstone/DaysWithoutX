{% extends 'JSomerstoneDaysWithout::layout.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="{{ content_classes }}">
            {% include 'JSomerstoneDaysWithout:Counter:newCounter.html.twig' %}
        </div>
    </div>
    <div class="row">
        <div class="{{ content_classes }}">
            <div class="col-xl-6">
                {% include 'JSomerstoneDaysWithout:Counter:counterList.html.twig'
                    with {
                        list_headline: 'Latest counters',
                        counters: latest,
                        showUser: true,
                        showContext: false
                    }
                %}
            </div>
            <div class="col-xl-6">
                {% include 'JSomerstoneDaysWithout:Counter:counterList.html.twig'
                    with {
                        list_headline: 'Resent resets',
                        counters: resentResets,
                        showUser: true,
                        showContext: false
                    }
                %}
            </div>
        </div>
    </div>
{% endblock %}
